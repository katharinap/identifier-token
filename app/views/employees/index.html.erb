<div class="clearfix">
    <div class="float-right">
	<%= link_to 'New Employee', new_company_employee_path(@company), class: "btn btn-primary btn-sm m-2" %>
    </div>
</div>

<%= render partial: "employee", collection: @employees.sort_by(&:full_name) %>

