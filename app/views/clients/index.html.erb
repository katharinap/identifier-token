<div class="clearfix">
    <div class="float-right">
	<%= link_to 'New Client', new_employee_client_path(@employee), class: "btn btn-primary btn-sm m-2" %>
    </div>
</div>

<%= render partial: "client", collection: @clients.sort_by(&:full_name)  %>

