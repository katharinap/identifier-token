<p id="notice"><%= notice %></p>

<h1>Clients</h1>
<h2>Company: <%= @company.name %></h2>
<h2>Employee: <%= @employee.full_name %></h2>

<%= render partial: "client", collection: @clients, locals: { show_employee: false }  %>

<%= link_to 'New Client', new_employee_client_path(@employee), class: "btn btn-secondary" %>
